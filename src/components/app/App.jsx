import React from 'react';
// import { Routes, Route } from 'react-router-dom';
import Button from '@mui/material/Button';

export default function App() {
    return (
        <div>
            {/* build Routes and Route here */}
            <Button variant="contained">Hello World</Button>
        </div>
    );
}
